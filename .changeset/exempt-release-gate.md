---
---

CI only — no package release. Exempts the Changesets release PR branch
(`changeset-release/*`) from the "changeset present" gate, so the automated
"Version Packages" PR passes CI once the org allows Actions to open PRs.
