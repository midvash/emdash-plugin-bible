---
---

Docs/CI only — no package release. Brings `CHANGELOG.md` in line with what 0.2.0
actually shipped (#29–#33: `/versions` unwrap, KV value coercion, a11y, peer-dep
widening) and adds a CI gate that requires every PR to include a changeset.
